import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // App basic colors
  static const Color primary = Color(0xFF525252);
  static const Color secondary =Color(0xFFF0EFDB);
  static const Color accent = Color(0xFFDADADA);

  // Text colors
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Colors.white;
  static const Color textAccent = Color(0xFF4C471D);

  // Background colors
  static const Color primaryBackground = Color(0xFFF0EFDB);

}